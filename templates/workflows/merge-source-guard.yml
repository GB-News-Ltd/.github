# Optional thin caller if you split guard from CI.
name: Merge source guard

on:
  pull_request:
    branches: [dev, staging, master]

permissions:
  contents: read
  pull-requests: write

jobs:
  guard:
    uses: GB-News-Ltd/.github/.github/workflows/merge-source-guard.yml@main
